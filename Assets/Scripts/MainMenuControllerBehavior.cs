using UnityEngine;
using System.Collections;

public class MainMenuControllerBehavior : MonoBehaviour {

	// Use this for initialization
	void Start() {
		// TODO(sean): Remove this and put it in the appropriate place
		// Level loading
		Application.LoadLevel(Scenes.GAMELOOPSCENE);
	}

	// Update is called once per frame
	void Update() {
		
	}

}
